<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 체크된 상태 -->
    <item
        android:state_checked="true"
        android:width="24dp"
        android:height="24dp"
        android:drawable="@drawable/checkbox_circle_checked" />
    <!-- 체크되지 않은 상태 -->
    <item
        android:drawable="@drawable/checkbox_circle_unchecked"
        android:width="24dp"
        android:height="24dp" />
</layer-list>